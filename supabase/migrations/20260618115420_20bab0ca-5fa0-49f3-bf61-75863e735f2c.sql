ALTER TABLE public.appointments ADD COLUMN IF NOT EXISTS external_id text;
CREATE UNIQUE INDEX IF NOT EXISTS appointments_user_external_uniq ON public.appointments(user_id, external_id) WHERE external_id IS NOT NULL;